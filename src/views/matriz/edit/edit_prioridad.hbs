<div class="d-flex justify-content-end p-2">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/dashboard">Inicio matriz</a></li>
            <li class="breadcrumb-item"><a href="/tables/prioridades">Tabla prioridades</a></li>
            <li class="breadcrumb-item">Editar prioridad {{PrioridadesEdit.nombre_prioridad}}</li>
        </ol>
    </nav>
</div>
<br>
<h3 class="modal-title col-md-6 mx-auto text-center bg-dark text-light" id="exampleModalLabel">Editar prioridad</h3>
<div class="card col-md-6 mx-auto">
    <div class="card-body rounded-0">
        <form action="/tables/prioridades/edit/{{PrioridadesEdit.id_prioridad}}" method="POST" name="form_prioridad">
            <div class="input-group">
                <input type="hidden" class="form-control" name="id_prioridad" id="id_prioridad"
                    value="{{PrioridadesEdit.id_prioridad}}" readonly="readonly">
            </div>
            <label for="nombre_persona">Nombre de la prioridad</label>
            <div class="input-group">
                <span class="input-group-text bg-transparent borderless_hallazgos">
                    <i class="fas fa-star"></i>
                </span>
                <input type="text" class="form-control" name="nombre_prioridad" id="nombre_prioridad"
                    value="{{PrioridadesEdit.nombre_prioridad}}" autofocus>
            </div>
            <br>
            <div class="modal-footer d-flex justify-content-center">
                <button type="button" class="btn btn-outline-success rounded-0" data-toggle="modal" data-target="#ModalEditarPrioridad{{@index}}">Editar</button>
                {{!-- MODAL EDITAR PRIORIDAD --}}
                <div class="modal fade" id="ModalEditarPrioridad{{@index}}" tabindex="-1"
                    aria-labelledby="EliminarUsuarioLabel" aria-hidden="true">
                    <div class="modal-dialog modal-dialog-centered modal-md">
                        <div class="modal-content rounded-0">
                            <div class="modal-body rounded-0">
                                <span class="text-primary d-flex justify-content-center">
                                    <i class="fas fa-info-circle fa-3x"></i>
                                </span>
                                <br>
                                <h5 class="text-center">¿Desea editar la prioridad<br>
                                    '{{PrioridadesEdit.nombre_prioridad}}'?</h5>
                                <br>
                                <div class="modal-footer d-flex justify-content-center">
                                    <button type="submit" class="btn btn-outline-success rounded-0">Sí
                                        (editar)</button>
                                    <button type="button" class="btn btn-outline-danger rounded-0"
                                        data-dismiss="modal">No (cerrar ventana)</button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                {{!-- FIN MODAL EDITA PRIORIDAD --}}
                <a type="button" class="btn btn-outline-danger rounded-0" href="/tables/prioridades">Cancelar</a>
            </div>
        </form>
    </div>
</div>